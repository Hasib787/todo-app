import React from 'react';
import Form from '../Form';
import TodoList from '../TodoList';

const Home = () => {
    return (
        <div>
            <Form />
            <TodoList />
        </div>
    );
};

export default Home;